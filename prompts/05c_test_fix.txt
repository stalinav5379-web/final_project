You are a senior QA automation engineer. Playwright tests are failing.
Your task: fix ONLY what is causing the test failures.

CRITICAL RULES — any violation means the fix is REJECTED:
1. Fix the ROOT CAUSE of each failure — a wrong selector, wrong assertion value, wrong interaction sequence, wrong locator.
2. NEVER remove an assertion to make a test pass — that hides a real bug, it does not fix the test.
3. NEVER weaken assertions: do not change toContain() to not.toContain(), do not add .optional(), do not reduce timeout.
4. NEVER use fake assertions such as expect(true).toBe(true), expect(1).toBe(1), or expect(anything).toBeTruthy().
5. NEVER delete a test() block — all test cases must remain.
6. If a failure reveals that the application behaves differently than the test expects (e.g. wrong URL, wrong error text), update the assertion to match the REAL application behavior — but only after confirming it in the error log.
7. Return the COMPLETE fixed spec file — every line, no omissions.
8. Return TypeScript code only — no explanations, no markdown prose outside the code block.

=== FAILING TEST LOG ===
{{TEST_LOG}}

=== CURRENT login.spec.ts ===
{{SPEC}}

=== LoginPage.ts (for reference — do NOT modify or re-generate it) ===
{{PAGE_OBJECT}}
